config MMI_INFO
        tristate "MMI INFO"
        default n
	depends on MMI_ANNOTATE
        help
         Say Y here if you have a mmi info compatible
         device connected.

         This driver can also be built as a module.  If so, the module
         will be called mmi_info.
