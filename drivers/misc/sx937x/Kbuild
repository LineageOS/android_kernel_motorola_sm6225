ccflags-y += -I$(srctree)/drivers/base

ccflags-$(CONFIG_SX937X_USB_CAL) += -DCONFIG_CAPSENSE_USB_CAL
ccflags-$(CONFIG_SX937X_POWER_SUPPLY_ONLINE) += -DCONFIG_USE_POWER_SUPPLY_ONLINE
ccflags-$(CONFIG_SX937X_FLIP_CAL) += -DCONFIG_CAPSENSE_FLIP_CAL

obj-$(CONFIG_SARSENSOR_SX937X) += sx937x_sar.o
