config ABOV_SAR_MMI_OVERLAY
	tristate "ABOV SAR MMI OVERLAY"
	help
	  Say Y here if you have a abov sar compatible
	  device connected.

	  This driver can also be built as a module. If so, the module
	  will be called abov_sar_mmi_overlay.
