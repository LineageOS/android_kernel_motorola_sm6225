ccflags-y += -I$(srctree)/drivers/base

ccflags-$(CONFIG_SX933X_USB_CAL) += -DCONFIG_CAPSENSE_USB_CAL
ccflags-$(CONFIG_SX933X_FLIP_CAL) += -DCONFIG_CAPSENSE_FLIP_CAL
ccflags-$(CONFIG_SX933X_ATTACH_CAL) += -DCONFIG_CAPSENSE_ATTACH_CAL
ccflags-$(CONFIG_SX933X_HEADSET_STATE) += -DCONFIG_CAPSENSE_HEADSET_STATE

obj-$(CONFIG_SARSENSOR_SX933X) += sx933x_sar.o
