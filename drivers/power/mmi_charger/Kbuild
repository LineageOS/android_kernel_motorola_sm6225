#ifeq ($(TARGET_BUILD_VARIANT),userdebug)
#    EXTRA_CFLAGS += -DMMI_GKI_API_ALLOWANCE
#endif

ccflags-$(CONFIG_MMI_RECHARGER_HAWAO_MODE) += -DCONFIG_MMI_RECHARGER_HAWAO_MODE

obj-$(CONFIG_MMI_CHARGER) += mmi_charger.o
